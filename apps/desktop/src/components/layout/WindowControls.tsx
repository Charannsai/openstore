'use client';

export default function WindowControls() {
  // Renders a reserved region for Windows Native TitleBar Overlay (WCO)
  return <div className="w-[138px] h-11 pointer-events-none select-none flex-shrink-0" />;
}
